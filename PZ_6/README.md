
# Проект: Генерация псевдослучайных чисел (ПСЧ) с использованием класса BigInt

## 📘 Описание проекта

Проект реализует генерацию **псевдослучайных чисел (ПСЧ)** различными методами на основе собственного класса **BigInt** (большие целые числа).
Используются три алгоритма генерации:

1. **Линейный конгруэнтный генератор (LCG)**
2. **Вихрь Мерсенна (MT19937, собственная реализация)**
3. **Метод накопления энтропии** — сбор энтропии из движений курсора (через X11 API под Linux/WSL2)

Полученные числа можно сохранять в CSV-файлы и строить графики распределения.

---

## 🧩 Структура проекта

```
PZ_6/
│
├── inc/
│   ├── bigint.h          # Класс BigInt — большие числа + методы генерации ПСЧ
│   ├── mt19937lab.h      # Собственная реализация вихря Мерсенна (структура MT19937lab)
│   ├── entropy.h         # Реализация метода накопления энтропии через координаты курсора
│   └── generate.h        # Генерация серий ПСЧ (100, 1000, 10000) и запись в CSV
│
├── src/
│   └── bigint.cpp        # Реализация класса BigInt и всех методов генерации
│
├── main/
│   └── main.cpp          # Построение графиков распределения на основе CSV (gnuplot)
│
├── data/                 # CSV-файлы с результатами генераций
│
├── CMakeLists.txt        # Сборочный файл проекта
└── README.md             # Текущее описание
```

---

## ⚙️ Методы генерации ПСЧ

### **1. initLCG** — линейный конгруэнтный генератор

Метод реализован **внутри класса BigInt**.

Перегрузки:

* `initLCG(size_t numDigits)` — генерирует число с указанным количеством цифр, сид берётся из системного времени.
* `initLCG(size_t numDigits, const BigInt& seed)` — генерация с пользовательским сидом.

---

### **2. initMT** — вихрь Мерсенна

Использует **собственную реализацию MT19937** (в файле `mt19937lab.h`), без стандартной библиотеки `<random>`.

Перегрузки:

* `initMT(size_t numDigits)` — случайный сид (по времени).
* `initMT(size_t numDigits, uint32_t seed)` — пользовательский сид.

---

### **3. initFromMouseEntropy** — метод накопления энтропии

Задействует библиотеку **`entropy.h`**, которая использует API **X11** для получения положения курсора на экране.

Перегрузки:

* `initFromMouseEntropy(size_t numDigits)` — сбор энтропии с дефолтными параметрами.
* `initFromMouseEntropy(size_t numDigits, int duration_ms, int poll_hz)` — задание длительности и частоты опроса.

---

## 📈 Генерация и анализ

### **generate.h**

* Генерирует по **100**, **1000**, **10000** чисел для каждого метода (LCG, MT, Entropy).
* Сохраняет результаты в **CSV**-файлы в папке `data/`.

### **main.cpp**

* Считывает CSV-файлы.
* С помощью **gnuplot** строит графики распределений для сравнения методов генерации.

---

## 🧰 Подготовка окружения (Linux / WSL2)

Перед сборкой проекта установи нужные пакеты:

### 1. Для работы с X11 (считывание позиции курсора)

```bash
sudo apt-get install -y libx11-dev
```

### 2. Для построения графиков

```bash
sudo apt install -y gnuplot
```

### 3. Проверить наличие CMake

```bash
cmake --version
```

Если нет:

```bash
sudo apt install -y cmake
```

---

## 🚀 Сборка и запуск проекта

1. Перейти в папку проекта:

```bash
cd ~/Bigint-Co/PZ_6
```

2. Очистить старую сборку (если была):

```bash
rm -rf build
```

3. Сконфигурировать и собрать:

```bash
cmake -S . -B build
cmake --build build -j
```

4. Запустить исполняемый файл:

```bash
./build/bigint_demo
```

---